spring:
  application:
    name: server
  datasource:
    driverClassName: org.postgresql.Driver
    url: 
    username: 
    password: 
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    ROOT: INFO



